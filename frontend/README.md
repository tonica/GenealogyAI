# GenealogyAI - Frontend (React)

Frontend de la aplicacion en React.

> Nota: el scaffolding de React (Vite, componentes, paginas) se implementara
> en una fase posterior. Esta carpeta esta reservada para el codigo del
> cliente web que consumira la API FastAPI expuesta por el backend.